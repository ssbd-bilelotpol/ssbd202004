CustomerService
ReportsPanelPage
ReportsController
ConnectionEndpoint
ConnectionService
ConnectionFacade
@Starter(CustomerService)

displaySuccess = ReportsPanelPage.generateReport() {
  successResponse = ReportsController.generateReport() {
    connections = ConnectionEndpoint.generateReport() {
      // RO, Read Committed
      while(1) {
        connections = ConnectionService.findAll() {
          connection = ConnectionFacade.findAll()
        }
      }

    }
  }
}
